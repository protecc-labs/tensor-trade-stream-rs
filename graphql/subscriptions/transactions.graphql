subscription NewTransactionTV2($slug: String!) {
  newTransactionTV2(slug: $slug) {
    tx {
      grossAmount
      mintOnchainId
      txAt
      txId
      txType
      buyerId
      sellerId
      source
    }
  }
}
